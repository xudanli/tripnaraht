# 智能体接口兼容性检查

## 改进内容

我们改进了 System 1 执行器在信息不足时的引导提示，从简单的"请提供更具体的操作指令"改为更友好的多行引导文本。

## 接口兼容性分析

### ✅ **接口不需要调整**

**原因：**

1. **类型兼容**
   - `answer_text` 字段类型：`string`（在 `RouteAndRunResponseDto` 中定义）
   - 我们返回的内容类型：`string`（多行文本，包含换行符 `\n`）
   - ✅ 完全兼容

2. **接口结构未变化**
   - 请求接口（`RouteAndRunRequestDto`）：无变化
   - 响应接口（`RouteAndRunResponseDto`）：无变化
   - 字段名称：无变化
   - 字段类型：无变化

3. **向后兼容**
   - 原有的响应格式完全兼容
   - 只是 `answer_text` 字段的内容更丰富了
   - 前端无需修改即可显示（多行文本会正确显示换行）

### 当前接口定义

```typescript
export class RouteAndRunResponseDto {
  result!: {
    status: 'OK' | 'NEED_MORE_INFO' | 'NEED_CONSENT' | 'NEED_CONFIRMATION' | 'FAILED' | 'TIMEOUT';
    answer_text: string;  // ✅ string 类型，完全兼容我们的改进
    payload: {
      timeline: any[];
      dropped_items: any[];
      candidates: any[];
      evidence: any[];
      robustness: any;
    };
  };
}
```

### 改进后的返回内容

**有行程ID时：**
```
我可以帮您：

• **添加地点**：例如"添加东京塔"或"在行程中加入浅草寺"
• **删除地点**：例如"删除浅草寺"或"移除东京塔"
• **查询地点**：例如"推荐新宿的拉面店"或"附近有什么景点"
• **规划行程**：例如"规划5天东京游"或"帮我规划行程"

请告诉我您想要做什么？
```

**无行程ID时：**
```
我可以帮您：

• **规划行程**：例如"规划5天东京游"或"帮我规划冰岛7日行程"
• **查询地点**：例如"推荐新宿的拉面店"或"东京有什么好玩的"
• **搜索景点**：例如"搜索东京塔"或"查找浅草寺"
• **创建行程**：告诉我目的地、日期和偏好，我来为您规划

请告诉我您想要做什么？
```

## 前端显示建议

虽然接口不需要调整，但前端可以考虑：

1. **保留换行显示**
   - 使用 `white-space: pre-line` 或 `white-space: pre-wrap` CSS 样式
   - 确保 `\n` 换行符正确显示

2. **格式化文本**
   - 可以解析 `**粗体**` Markdown 语法（如果前端支持）
   - 或者使用简单的文本显示（粗体标记会显示为纯文本，不影响理解）

3. **示例高亮**
   - 可以识别"例如"后面的示例文本，进行特殊样式显示

## 总结

✅ **接口不需要调整**
- 类型完全兼容（`string` 类型）
- 接口结构未变化
- 向后兼容

✅ **改进已生效**
- 引导提示更友好
- 提供了具体的操作示例
- 提升了用户体验

✅ **前端兼容**
- 标准 `string` 类型，前端可以直接显示
- 建议使用 `white-space: pre-line` 保留换行
