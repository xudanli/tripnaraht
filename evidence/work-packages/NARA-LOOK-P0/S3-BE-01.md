# S3-BE-01 — Grounding + reconciliation

**Status:** IMPLEMENTED (hint-backed ports; live Trip/Road providers next)  
**Date:** 2026-07-25  
**Parent:** [`PROCESS_STATUS.md`](./PROCESS_STATUS.md)

---

## Delivered

| Item | Path |
|------|------|
| Context builder | `grounding/context-builder.ts` |
| Geo match | `grounding/geo-grounding.ts` |
| Vehicle–road fit | `grounding/vehicle-road-fit.ts` |
| Meeting point | `grounding/meeting-point.ts` |
| Reconciliation | `grounding/state-reconciliation.ts` |
| Service | `grounding/observation-grounding.service.ts` |
| Pipeline | EXTRACTING → GROUNDING → ASSESSING → COMPLETED |
| Tests | `grounding.contract.spec.ts` + observation contracts |

---

## Behavior

```text
visual facts
  + groundingHints (vehicle / nearby roads / roadStatuses / booking)
  → ObservationContext
  → roadMatch / vehicleRoadFit / meetingPoint
  → verificationStatus + Semantic Key facts
  → Assessment
```

| Case | Result |
|------|--------|
| F208 + highland GPS + 2WD + planned F-road | `RULE_TRIGGER.FROAD_VEHICLE_MISMATCH` → **EXECUTION_BLOCK** + PREVIEW |
| F208 OCR + Reykjavik GPS | `DATA_CONFLICT.IMAGE_LOCATION_MISMATCH` → CONFLICTING |
| Closed sign vs official OPEN | `DATA_CONFLICT.ROAD_STATUS_CONFLICT` |
| Operator ≠ booking | `EXECUTION_DEVIATION.WRONG_MEETING_POINT` |
| No GPS | still no road EXECUTION_BLOCK (Q5) |

`writesPlanVersion = false`. Preview ref is opaque (`arrange:froad_vehicle_mismatch`) — Apply remains existing corridors.

---

## Reproduce

```bash
LLM_USE_MOCK=true npx jest --runInBand --forceExit \
  src/trips/travel-observation/grounding/grounding.contract.spec.ts \
  src/trips/travel-observation/observation.contract.spec.ts
```

---

## Next

- **S4-BE-01** wire DecisionProblem / Unified Assessment / real Preview refs  
- Replace `groundingHints` with Trip Context + Road Status provider reads
