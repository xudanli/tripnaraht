# S4-BE-04 — Look facts → WorldState assertion (Observation Channel)

**Status:** IMPLEMENTED  
**Date:** 2026-07-25  
**Parent:** [`PROCESS_STATUS.md`](./PROCESS_STATUS.md)

---

## Delivered

| Item | Path |
|------|------|
| Pure projector | `assessment/project-look-to-world-state.ts` |
| Persist service | `assessment/look-world-state-assertion.service.ts` |
| Wire in finalize | `observation.service.ts` (before DecisionProblem attach) |
| Tests | `assessment/look-world-state-assertion.contract.spec.ts` |

---

## Invariants

| Rule | Enforcement |
|------|-------------|
| Predicate = `look.field_observation` | Builder + service guard |
| **Never** `road.status` | Throw if attempted; integration assert |
| `authoritative: false` | Payload literal |
| Provenance `USER` / `NARA_LOOK` | Source fields |
| Channel `LOOK_FIELD` | Payload |

Supersede key: `(subjectRef=look_obs:{observationId}:{semanticKey}, predicate=look.field_observation)` — recapture refreshes same fact without touching official road SSOT.

Snapshot from append is passed into RFC-001 DecisionProblem as `worldStateSnapshotId` when available.

---

## Reproduce

```bash
LLM_USE_MOCK=true npx jest --runInBand --forceExit \
  src/trips/travel-observation/assessment/look-world-state-assertion.contract.spec.ts
```

---

## Related bans

[`FORBIDDEN_PARALLEL_WRITES.md`](./FORBIDDEN_PARALLEL_WRITES.md) #3 — OCR must not patch authoritative road SSOT; Look assertions are user-observed only.
