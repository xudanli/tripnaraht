# S4-BE-03 — Look RFC projection context + Decision read-model invalidate

**Status:** IMPLEMENTED  
**Date:** 2026-07-25  
**Parent:** [`PROCESS_STATUS.md`](./PROCESS_STATUS.md) · depends on [`S4-BE-02.md`](./S4-BE-02.md)

---

## Delivered

| Item | Path |
|------|------|
| Context resolver | `assessment/look-trip-decision-context.resolver.ts` |
| Projection + invalidate | `assessment/look-rfc001-projection.service.ts` |
| Module imports | Prisma + GuardianDecisionCore + DecisionGateway |
| Tests | `assessment/look-rfc001-projection.contract.spec.ts` |

---

## Behavior

```text
Look DecisionProblem upsert
  → resolve planVersionId
       1. Rfc001PlanVersionStore.getEffectivePlanVersionId
       2. else plan_{tripRevision}
       3. else PLAN_VERSION_PENDING_LOOK
  → resolve worldStateSnapshotId
       1. latest WorldStateStore snapshot
       2. else ws_look_{observationId}
  → Rfc001DecisionProblemStore.upsert
  → UnifiedDecisionProblemReadModel.invalidateCache(tripId)  (if gateway unified ON)
```

**Still true:** no PlanVersion write from Look; Preview / Confirm only via Decision Gateway / UWC.

---

## Reproduce

```bash
LLM_USE_MOCK=true npx jest --runInBand --forceExit \
  src/trips/travel-observation/assessment/look-rfc001-projection.contract.spec.ts
```

---

## Next (out of this ticket)

- **S4-BE-04** ✅ Look facts → WorldState (`look.field_observation`)
- iOS Decision Space deep-link for `decision:{id}`
- Legal TTL + SIGNATURES for Pilot ship
