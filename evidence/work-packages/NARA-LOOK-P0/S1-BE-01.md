# S1-BE-01 — Observation API + status machine + 409

**Status:** IMPLEMENTED (S1 scoped mock)  
**Date:** 2026-07-25  
**Parent:** [`PROCESS_STATUS.md`](./PROCESS_STATUS.md)

---

## Delivered

| Item | Path |
|------|------|
| Types | `src/trips/travel-observation/observation.types.ts` |
| Semantic keys | `src/trips/travel-observation/semantic-keys.ts` |
| CTA / roles | `src/trips/travel-observation/cta-and-roles.ts` |
| Retention | `src/trips/travel-observation/media-retention.ts` |
| Status machine | `src/trips/travel-observation/observation-status.machine.ts` |
| Service + repo | `observation.service.ts` / `observation.repository.ts` |
| HTTP | `GET/POST/DELETE …/api/v1/trips/:tripId/observations` |
| Module | `TravelObservationModule` wired in `trips.module.ts` |
| Contract tests | `observation.contract.spec.ts` |

---

## HTTP

```text
POST   /api/v1/trips/:tripId/observations
GET    /api/v1/trips/:tripId/observations/:observationId
GET    /api/v1/trips/:tripId/observations/:observationId/assessment  → 409 until COMPLETED
POST   /api/v1/trips/:tripId/observations/:observationId/media
DELETE /api/v1/trips/:tripId/observations/:observationId
```

**No Apply endpoint.**

---

## S1 limits

- Assessment is **mock** (no multimodal / OCR) — S2  
- Store is **in-memory** — durable store follow-on  
- Trip membership auth not wired (Public + tripId scoping only) — harden with access service next  

---

## Reproduce

```bash
LLM_USE_MOCK=true npx jest --runInBand --forceExit \
  src/trips/travel-observation/observation.contract.spec.ts
```
