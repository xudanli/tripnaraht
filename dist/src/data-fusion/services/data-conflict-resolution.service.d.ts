import { ConflictReport, FusedData, DataSourceConfig, FusionConfig, FusionResult } from '../interfaces/data-fusion.interface';
import { FusionResilienceService } from './fusion-resilience.service';
import { FusionResourceManagerService } from './fusion-resource-manager.service';
export declare class DataConflictResolutionService {
    private readonly resilienceService?;
    private readonly resourceManager?;
    private readonly logger;
    private readonly conflictCache;
    private readonly CACHE_TTL;
    private readonly PARALLEL_THRESHOLD;
    private readonly performanceStats;
    constructor(resilienceService?: FusionResilienceService, resourceManager?: FusionResourceManagerService);
    detectConflicts(dataSources: DataSourceConfig[]): ConflictReport;
    private generateCacheKey;
    private cleanExpiredCache;
    reliabilityWeightedFusion(dataSources: DataSourceConfig[], field?: string): FusedData;
    prioritySelection(dataSources: DataSourceConfig[], priorityOrder?: string[]): FusedData;
    contextBasedSelection(dataSources: DataSourceConfig[], context: Record<string, any>): FusedData;
    fuse(dataSources: DataSourceConfig[], config?: FusionConfig): Promise<FusionResult>;
    private collectFieldValues;
    private detectFieldConflict;
    private determineConflictType;
    private determineConflictSeverity;
    private generateConflictDescription;
    private assessConflictImpact;
    private suggestResolutionStrategy;
    private fuseValues;
    private reliabilityWeightedFuseValues;
    private averageFuseValues;
    private medianFuseValues;
    private modeFuseValues;
    private extractFieldValues;
    private extractAllFieldValues;
    private getAllFields;
    private collectFields;
    private getNestedValue;
    private setNestedValue;
    private calculateContextScore;
    private fuzzyMatch;
    private averageFusion;
    private medianFusion;
    private calculateQualityMetrics;
    private generateRecommendations;
    private generateConflictSummary;
    private updateAverageTime;
}
