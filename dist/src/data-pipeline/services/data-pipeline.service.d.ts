import { DataQualityFrameworkService } from '../../data-quality/services/data-quality-framework.service';
import { DataPrivacyFrameworkService } from '../../data-privacy/services/data-privacy-framework.service';
import { DataCleaningService } from './data-cleaning.service';
import { DataStandardizationService } from './data-standardization.service';
import { CollectedData, ProcessedData, CollectionTaskConfig } from '../interfaces/data-pipeline.interface';
import { PipelineDefinition, PipelineStep, PipelineExecutionResult, PipelineMonitoringConfig } from '../interfaces/pipeline-definition.interface';
export declare class DataPipelineService {
    private readonly dataCleaningService;
    private readonly dataStandardizationService;
    private readonly dataQualityFramework?;
    private readonly dataPrivacyFramework?;
    private readonly logger;
    constructor(dataCleaningService: DataCleaningService, dataStandardizationService: DataStandardizationService, dataQualityFramework?: DataQualityFrameworkService, dataPrivacyFramework?: DataPrivacyFrameworkService);
    dataCollectionPipeline(collectionTasks?: Record<string, CollectionTaskConfig>): Promise<CollectedData>;
    dataProcessingPipeline(rawData: CollectedData): Promise<ProcessedData>;
    dataApplicationPipeline(processedData: ProcessedData): Promise<void>;
    processDataFlow(userInput?: Record<string, any>, collectionTasks?: Record<string, CollectionTaskConfig>): Promise<ProcessedData>;
    private fetchData;
    private validateSchema;
    private logValidationError;
    private cleanData;
    private standardizeData;
    private extractInferenceFeatures;
    private extractRiskFeatures;
    private extractDecisionFeatures;
    private prepareUIData;
    private sendToInferenceEngine;
    private sendToRiskSystem;
    private sendToDecisionSystem;
    private sendToUI;
    private logDecisionData;
    createPipelineDefinition(name: string, steps: Array<Omit<PipelineStep, 'id' | 'status'>>, description?: string): PipelineDefinition;
    executePipeline(definition: PipelineDefinition, inputData?: any, monitoringConfig?: PipelineMonitoringConfig): Promise<PipelineExecutionResult>;
    private resolveExecutionOrder;
    private executePipelineStep;
    private handlePipelineStepError;
    private calculatePipelineQualityMetrics;
    private generatePipelineRecommendations;
}
