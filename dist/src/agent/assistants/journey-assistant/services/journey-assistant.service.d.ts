import { LlmService } from '../../../../llm/services/llm.service';
import { ExecutionAgentService } from '../../../services/execution-agent.service';
import { PersonaShellService } from '../../../services/persona-shell.service';
import { PrismaService } from '../../../../prisma/prisma.service';
import { LLMExecutorService } from '../../../infra/llm-executor.service';
import { CoreGatewayService } from '../../../infra/core-gateway.service';
import { JourneyAssistantRequest, JourneyAssistantResponse } from '../interfaces/journey-assistant.interface';
export declare class JourneyAssistantService {
    private readonly coreGateway?;
    private readonly llmExecutor?;
    private readonly prisma?;
    private readonly llmService?;
    private readonly executionAgent?;
    private readonly personaShell?;
    private readonly logger;
    private journeyStates;
    private notificationQueue;
    constructor(coreGateway?: CoreGatewayService, llmExecutor?: LLMExecutorService, prisma?: PrismaService, llmService?: LlmService, executionAgent?: ExecutionAgentService, personaShell?: PersonaShellService);
    handle(request: JourneyAssistantRequest): Promise<JourneyAssistantResponse>;
    private handleChat;
    private handleGetStatus;
    private handleGetReminders;
    private handleEvent;
    private handleAdjustSchedule;
    private handleNearbySearch;
    private handleScheduleQuery;
    private handleNavigation;
    private handleRecommendation;
    private handleEmergencyChat;
    private handleAdjustmentChat;
    private handleGeneralChat;
    proactiveReminderCheck(): Promise<void>;
    private generateReminders;
    private generatePreTripReminders;
    private generateDepartureDayReminders;
    private generateOnTripReminders;
    private generateReturnDayReminders;
    private loadJourneyState;
    private saveJourneyState;
    private generateSampleSchedule;
    private analyzeIntent;
    private generateStatusMessage;
    private translatePhase;
    private getSuggestedActions;
    private getCurrentActivity;
    private generateEmergencyOptions;
    private executeSelectedOption;
    private generateEventMessage;
    private translateSeverity;
    private extractSearchType;
    private translateSearchType;
    private getActiveTrips;
    private sendPushNotifications;
    private createErrorResponse;
}
