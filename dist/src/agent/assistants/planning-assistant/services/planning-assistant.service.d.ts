import { LlmService } from '../../../../llm/services/llm.service';
import { PlanningWorkbenchAgentService } from '../../../services/planning-workbench-agent.service';
import { PersonaShellService } from '../../../services/persona-shell.service';
import { PrismaService } from '../../../../prisma/prisma.service';
import { PersonaLanguageService } from '../../shared/services/persona-language.service';
import { RecommendationEngineService } from '../../shared/services/recommendation-engine.service';
import { PreferenceLearningService } from '../../shared/services/preference-learning.service';
import { LLMExecutorService } from '../../../infra/llm-executor.service';
import { CoreGatewayService } from '../../../infra/core-gateway.service';
import { PlanningConversationState, PlanningAssistantRequest, PlanningAssistantResponse } from '../interfaces/planning-assistant.interface';
export declare class PlanningAssistantService {
    private readonly coreGateway?;
    private readonly llmExecutor?;
    private readonly llmService?;
    private readonly planningWorkbench?;
    private readonly personaShell?;
    private readonly prisma?;
    private readonly personaLanguage?;
    private readonly recommendationEngine?;
    private readonly preferenceLearning?;
    private readonly logger;
    private sessions;
    private readonly SESSION_TTL_MS;
    constructor(coreGateway?: CoreGatewayService, llmExecutor?: LLMExecutorService, llmService?: LlmService, planningWorkbench?: PlanningWorkbenchAgentService, personaShell?: PersonaShellService, prisma?: PrismaService, personaLanguage?: PersonaLanguageService, recommendationEngine?: RecommendationEngineService, preferenceLearning?: PreferenceLearningService);
    chat(request: PlanningAssistantRequest): Promise<PlanningAssistantResponse>;
    createSession(userId?: string): Promise<string>;
    getSessionState(sessionId: string): Promise<PlanningConversationState | null>;
    private analyzeIntentWithLLM;
    private analyzeIntentByKeywords;
    private handleRecommendWithReadiness;
    private handleGeneratePlanWithWorkbench;
    private handleConfirmAndSaveTrip;
    private handleQuestionWithLLM;
    private handleQuestionDefault;
    private handleGeneralWithLLM;
    private handleGeneralDefault;
    private handleExplore;
    private handleCollectInfo;
    private handleCompare;
    private handleAdjust;
    private loadOrCreateSession;
    private saveSession;
    private addMessage;
    private createErrorResponse;
    private extractPreferences;
    private parseMonth;
    private getMissingInfo;
    private translateMissingInfo;
    private translatePace;
    private extractDestination;
    private extractSelectedPlanId;
    private getDefaultStartDate;
    private getDefaultEndDate;
    private generateMatchReasons;
    private generateMatchReasonsCN;
    private getDefaultRecommendations;
    private getDefaultPlanCandidates;
    getUserPreferenceSummary(userId: string): Promise<{
        summary: string;
        summaryCN: string;
        topPreferences: {
            label: string;
            labelCN: string;
            value: string;
        }[];
        learnedPreferences?: any;
    }>;
    clearUserPreferences(userId: string): Promise<void>;
}
