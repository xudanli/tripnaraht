import { OnModuleInit } from '@nestjs/common';
import { AirbnbService } from '../../../../mcp/airbnb.service';
import { WeatherDirectService } from '../../../../mcp/weather-direct.service';
import { ExaService } from '../../../../mcp/exa.service';
import { GoogleCalendarService } from '../../../../mcp/google-calendar.service';
import { GoogleMapsDirectService } from '../../../../mcp/google-maps-direct.service';
import { HotelDirectService } from '../../../../mcp/hotel-direct.service';
import { AdvancedGeocodingService } from './advanced-geocoding.service';
export declare class McpToolDispatcherService implements OnModuleInit {
    private readonly airbnbService?;
    private readonly weatherDirectService?;
    private readonly exaService?;
    private readonly googleCalendarService?;
    private readonly googleMapsDirectService?;
    private readonly hotelDirectService?;
    private readonly advancedGeocodingService?;
    private readonly logger;
    private readonly locationNameMap;
    private readonly geocodeCache;
    private readonly GEOCODE_CACHE_TTL;
    constructor(airbnbService?: AirbnbService, weatherDirectService?: WeatherDirectService, exaService?: ExaService, googleCalendarService?: GoogleCalendarService, googleMapsDirectService?: GoogleMapsDirectService, hotelDirectService?: HotelDirectService, advancedGeocodingService?: AdvancedGeocodingService);
    onModuleInit(): void;
    executeTool(serviceName: string, toolName: string, params: Record<string, any>, retries?: number): Promise<any>;
    private isRetryableError;
    private executeAirbnbTool;
    private executeWeatherTool;
    private executeHotelTool;
    private executeExaTool;
    private executeGoogleCalendarTool;
    private parseDateTime;
    isServiceAvailable(serviceName: string): boolean;
    private getDefaultStartDate;
    private getDefaultEndDate;
    private normalizeLocationName;
    private normalizeLocationNameBasic;
    private cleanExpiredGeocodeCache;
    private getCountryNameFromCode;
    private getCountryCenterCoordinates;
}
