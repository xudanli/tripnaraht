import { AgentState } from '../interfaces/agent-state.interface';
import { ActionRegistryService } from './action-registry.service';
import { CriticService } from './critic.service';
import { AgentStateService } from './agent-state.service';
import { EventTelemetryService } from './event-telemetry.service';
import { ActionCacheService } from './action-cache.service';
import { ActionDependencyAnalyzerService } from './action-dependency-analyzer.service';
import { LlmPlanService } from './llm-plan-service';
import { TripNaraSystemPromptService } from './tripnara-system-prompt.service';
import { AgentResumeService } from '../../trips/decision/services/agent-resume.service';
import { TelemetryService } from '../infra/telemetry.service';
import { AuditLogService } from '../infra/audit-log.service';
export declare class OrchestratorService {
    private actionRegistry;
    private critic;
    private stateService;
    private eventTelemetry?;
    private actionCache?;
    private dependencyAnalyzer?;
    private llmPlan?;
    private systemPromptService?;
    private agentResumeService?;
    private telemetry?;
    private auditLog?;
    private readonly logger;
    constructor(actionRegistry: ActionRegistryService, critic: CriticService, stateService: AgentStateService, eventTelemetry?: EventTelemetryService, actionCache?: ActionCacheService, dependencyAnalyzer?: ActionDependencyAnalyzerService, llmPlan?: LlmPlanService, systemPromptService?: TripNaraSystemPromptService, agentResumeService?: AgentResumeService, telemetry?: TelemetryService, auditLog?: AuditLogService);
    execute(state: AgentState, budget: {
        max_seconds: number;
        max_steps: number;
        max_browser_steps: number;
    }): Promise<AgentState>;
    private plan;
    private extractUrlFromInput;
    private actWithCacheInfo;
    private act;
    private actParallel;
    private observe;
    private repair;
    private updateStateFromAction;
    private shouldContinue;
    private isTimeout;
    private markNeedMoreInfo;
    private addMinutes;
    private compareTime;
    private isHardInfeasible;
    private getReasonCode;
    private extractFacts;
}
