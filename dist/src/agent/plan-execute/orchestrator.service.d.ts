import { PlannerService } from './planner.service';
import { ReplannerService } from './replanner.service';
import { ExecutorService } from './executor.service';
import { ContextAssemblerService } from './context-assembler.service';
import { AgentStateService } from '../services/agent-state.service';
import { OrchestrationResult } from './types';
import { LlmService } from '../../llm/services/llm.service';
import { TrajectoryCollectionService } from '../training/services/trajectory-collection.service';
import { RLIntegrationService } from '../training/services/rl-integration.service';
export declare class DAGOrchestratorService {
    private readonly planner;
    private readonly replanner;
    private readonly executor;
    private readonly contextAssembler;
    private readonly agentStateService?;
    private readonly llmService?;
    private readonly trajectoryCollection?;
    private readonly rlIntegration?;
    private readonly logger;
    private executionContext;
    private readonly maxSteps;
    private readonly maxIterations;
    private readonly maxReplanAttempts;
    constructor(planner: PlannerService, replanner: ReplannerService, executor: ExecutorService, contextAssembler: ContextAssemblerService, agentStateService?: AgentStateService, llmService?: LlmService, trajectoryCollection?: TrajectoryCollectionService, rlIntegration?: RLIntegrationService);
    run(threadId: string, userGoal: string, executionContext?: {
        tripId?: string | null;
        userId?: string;
        requestId?: string;
    }): Promise<OrchestrationResult>;
    private findRunnableTasks;
    private executeTaskWrapper;
    private resolveVariableReferences;
    private getNestedValue;
    private buildDependencyContext;
    private summarizeData;
    private detectDeadlock;
    private detectCycle;
    private generateFinalSummary;
    private getLlmProvider;
}
