import { PlanRequest, OptimizationResult } from '../interfaces/plan-request.interface';
import { RobustTimeMatrixService } from './robust-time-matrix.service';
import { ExplanationService } from './explanation.service';
import { DataExpiryPolicyService, TimestampedData } from './data-expiry-policy.service';
import { ConservativeStrategyService } from './conservative-strategy.service';
import { MetricsAggregatorService } from './metrics-aggregator.service';
export declare class EnhancedVRPTWOptimizerService {
    private robustTimeMatrixService;
    private explanationService;
    private dataExpiryPolicyService;
    private conservativeStrategyService;
    private metricsAggregatorService;
    private readonly logger;
    constructor(robustTimeMatrixService: RobustTimeMatrixService, explanationService: ExplanationService, dataExpiryPolicyService: DataExpiryPolicyService, conservativeStrategyService: ConservativeStrategyService, metricsAggregatorService: MetricsAggregatorService);
    solve(request: PlanRequest, options?: {
        request_id?: string;
        data_sources?: {
            dem?: TimestampedData<any>;
            transport?: TimestampedData<any>;
            opening_hours?: TimestampedData<Record<string, any>>;
            weather?: TimestampedData<any>;
            poi?: TimestampedData<Record<string, any>>;
        };
    }): Promise<OptimizationResult>;
    private applyPacingPreference;
    private getPacingConfig;
    private checkEarlyDepartureConstraints;
    private createInfeasibleResult;
    private expandMultiTimeWindows;
    private buildDisjunctionGroups;
    private solveWithHeuristic;
    private greedyConstructionWithDrops;
    private canVisitNode;
    private visitNode;
    private calculateArrivalTime;
    private calculateWaitTime;
    private calculateNodeScore;
    private parseTimeToMinutes;
    private postProcess;
    private generateTimeline;
    private generateRobustnessMetadata;
    private calculateTravelTimeBetweenNodes;
    private calculateDistance;
    private toRadians;
    private determineDropReasonCode;
    private buildDropContext;
    private calculateDropPenalty;
    private calculateRobustnessScore;
    private generateDiagnostics;
    private minutesToTimeString;
    private applyConservativeConstraints;
    private recordExecution;
}
