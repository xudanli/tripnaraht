-- Historical migration name present on production `_prisma_migrations` as `0000_baseline`.
-- No DDL: baseline schema was applied outside this file / superseded by later migrations.
-- Kept so local `prisma/migrations` matches the database migration history (drift reconciliation).

SELECT 1;
