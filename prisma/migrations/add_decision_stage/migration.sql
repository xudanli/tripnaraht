-- Legacy migration name present on production `_prisma_migrations` as `add_decision_stage`
-- (without timestamp prefix). The substantive DDL lives in `20250114000000_add_decision_stage`.
-- No-op here so local folder names match `_prisma_migrations` for Prisma history reconciliation.

SELECT 1;
