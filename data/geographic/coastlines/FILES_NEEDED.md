# 需要哪些文件？

## 📋 从你的海岸线数据文件夹中复制

### ✅ 必需：海岸线数据

**复制以下文件到 `data/geographic/coastlines/`：**

```
海岸线数据文件夹/
├── lines.shp  ✅ 必需
├── lines.shx  ✅ 必需
├── lines.dbf  ✅ 必需
├── lines.prj  ✅ 必需（非常关键！）
└── lines.cpg  （可选）
```

### 📁 目标位置

复制后，文件应该在这里：

```
data/geographic/coastlines/
├── lines.shp
├── lines.shx
├── lines.dbf
└── lines.prj
```

### ⚠️ 重要提示

1. **`.prj` 文件必需**：没有这个文件，无法正确识别坐标系
2. **4个文件缺一不可**：`.shp`, `.shx`, `.dbf`, `.prj`
3. **保持文件名一致**：不要重命名文件

### 🚀 导入命令

文件放置好后，运行：

```bash
npx ts-node --project tsconfig.backend.json scripts/import-coastlines-to-postgis.ts
```

