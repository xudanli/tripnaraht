# Agent Skills 导出目录

由 `npm run skill-evolver:export-agent-skills` 生成，格式见 [agentskills.io/specification](https://agentskills.io/specification.md)。

- 勿手改：请改 `data/skill-evolver/current/` 或 `artifacts/country-pack/` 后重新导出
- `manifest.json` 记录 `tripnaraSkillId` ↔ `agentSkillsName` 映射
