/**
 * @deprecated 实现已迁至 `src/agent/routing/gateway-route-policy.util.ts`（RequestRouter L1）。
 * 保留本文件 re-export，避免打断既有 import / golden / shadow 评测。
 */
export {
  routePolicy,
  type OrchestrationPolicyDecision,
} from '../routing/gateway-route-policy.util';
