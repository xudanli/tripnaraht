/**
 * P-ECO-Closure-10 — Neptune-facing existential audit (identity-preserving reflective kernel summary).
 */

export interface ExistentialAssessment {
  identityStable: boolean;
  continuityScore: number;
  mutationRisk: number;
  ontologicalIntegrity: number;
}
