/**
 * P-ECO-Closure-8 — Reflective semantic overlay on Neptune / closure audit (no mandatory Neptune codepath change).
 */

export interface NeptuneReflectiveSemanticAugmentation {
  reasoningConfidence: number;
  semanticSelfAssessment: number;
  reflectiveUncertainty: number;
}
