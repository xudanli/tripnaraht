/**
 * @deprecated Import from `src/common/utils/format-clock-label.util` instead.
 * Re-export kept for existing trip-constraint-solver call sites.
 */
export {
  DEFAULT_TRIP_DISPLAY_TIMEZONE,
  formatClockLabel,
  formatClockLabelOptional,
  parseClockHour,
  type FormatClockLabelOptions,
} from '../../../common/utils/format-clock-label.util';
