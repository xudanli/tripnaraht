---
name: verify
description: >-
  TripNARA VERIFY 主线（显式 /verify 调用）。
disable-model-invocation: true
---

# /verify — VERIFY 工程上下文

用户已通过 **`/verify`** 唤起。在继续前用 Read 工具读取：

1. `.cursor/capabilities/verify-mainline/SKILL.md` 全文  

**职责域**：`verify-executor`、`itinerary.verify`、RouteFeasibilityEngine、与内核 verify–repair 环；Plan 后必验。
