---
name: kernel
description: >-
  TripNARA 决策内核 Decision Kernel（显式 /kernel 调用）。
disable-model-invocation: true
---

# /kernel — 决策内核工程上下文

用户已通过 **`/kernel`** 唤起。在继续前用 Read 工具读取：

1. `.cursor/capabilities/decision-kernel-engineering/SKILL.md` 全文  

**职责域**：`decision-kernel.service`、`StateManager`、DSO patch、adapters、`candidate-search.pipeline`、execute* 阶段语义。
