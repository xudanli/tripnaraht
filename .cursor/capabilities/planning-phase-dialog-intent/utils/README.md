# planning-phase-dialog-intent utils

实现代码在 **`src/agent/utils/planning-intent-processor.util.ts`**（Jest 根目录为 `src/`，与 `route-and-run-intent-analyzer.util.ts` 同级）。

```bash
npx jest src/agent/utils/planning-intent-processor.util.spec.ts
```

主指南：[`../SKILL.md`](../SKILL.md)
