# 规划工作台 - 人格外壳设计

## 设计原则

**面向用户只显示"三人格"作为可解释与信任的"人格外壳"，其他角色（预算/交通/节奏/总规划师）都隐藏成能力模块。**

## 架构设计

### 三层结构

```
用户界面层
  ↓
人格外壳层（PersonaShellService）
  - 只显示三人格（Abu/Dr.Dre/Neptune）
  - 将底层能力模块的结果包装成三人格的决策
  ↓
能力模块层（隐藏）
  - 总规划师（architect）
  - 预算规划师（budget）
  - 交通规划师（transit）
  - 节奏规划师（pace）
  - 安全守门人（gate）
```

### 人格映射

#### Abu（🐻‍❄️ 安全否决者）
**整合的能力模块：**
- 预算规划师：预算超支检测 → Abu 的安全检查
- 交通规划师：不可达检测 → Abu 的硬约束检查
- 安全守门人：门控预检查 → Abu 的合规检查

**用户看到的：**
- "Abu 说：预算超支 2000 元，这在当前约束下存在明显风险。"
- "Abu 说：经过安全检查，当前方案在物理现实和合规性方面没有问题。"

#### Dr.Dre（🐕 节奏修复者）
**整合的能力模块：**
- 节奏规划师：疲劳评分 → Dr.Dre 的节奏评估
- 节奏规划师：时间窗计算 → Dr.Dre 的可用时间评估
- 节奏规划师：节奏调整 → Dr.Dre 的调整建议

**用户看到的：**
- "Dr.Dre 说：当前节奏的疲劳评分是 85/100，明显过高。原本的节奏会让你在行程中后期明显疲劳。"
- "Dr.Dre 说：当前节奏合理，每一天都刚刚好，体验稳定。"

#### Neptune（🦦 空间修复者）
**整合的能力模块：**
- 总规划师：方案生成 → Neptune 的路线哲学
- 交通规划师：PlanB 生成 → Neptune 的替代方案
- 安全守门人：替代方案 → Neptune 的空间修复

**用户看到的：**
- "Neptune 说：路线本身没有问题，但部分路段需要替换。我为你准备了替代方案，你走的仍然是同一条路线，体验不会打折扣。"
- "Neptune 说：当前方案在空间和路线哲学方面没有问题，所有路段都可行。"

## 实现细节

### PersonaShellService

**文件**: `src/agent/services/persona-shell.service.ts`

**核心方法：**
- `wrapAsPersonas(planState: PlanState)`: 将 PlanState 转换为三人格输出

**输出结构：**
```typescript
interface PersonaShellOutput {
  personas: {
    abu: PersonaStatement | null;
    drdre: PersonaStatement | null;
    neptune: PersonaStatement | null;
  };
  consolidatedDecision: {
    status: 'ALLOW' | 'NEED_CONFIRM' | 'REJECT';
    summary: string;
    nextSteps: string[];
  };
  timestamp: string;
}
```

### PersonaStatement

每个三人格的陈述包含：
- `persona`: 人格名称（ABU/DR_DRE/NEPTUNE）
- `icon`: 人格图标（🐻‍❄️/🐕/🦦）
- `slogan`: 人格标语
- `verdict`: 决策结论（ALLOW/ADJUST/REPLACE/REJECT/NEED_CONFIRM）
- `explanation`: 面向用户的解释（第一人称）
- `evidence`: 证据引用
- `recommendations`: 建议的行动
- `confirmations`: 需要用户确认的点

### 整合逻辑

#### Abu 的整合逻辑
1. **预算超支检测**（来自预算规划师）
   - 如果超支 > 20% → REJECT
   - 如果超支 < 20% → NEED_CONFIRM
   - 生成 Abu 的解释："预算超支 X 元，这在当前约束下存在明显风险。"

2. **交通不可达检测**（来自交通规划师）
   - 如果有不可达段 → REJECT
   - 生成 Abu 的解释："X 段跨城交通不可达，这在当前时间窗口下无法执行。"

3. **门控状态**（来自安全守门人）
   - 如果 REJECT → Abu 的 verdict = REJECT
   - 如果 NEED_CONFIRM → Abu 的 verdict = NEED_CONFIRM

#### Dr.Dre 的整合逻辑
1. **疲劳评分**（来自节奏规划师）
   - 如果 > 85 → ADJUST，建议插入休息日
   - 如果 70-85 → ADJUST，建议优化时间分配
   - 如果 < 70 → ALLOW

2. **时间窗**（来自节奏规划师）
   - 如果可用时间 < 6 小时 → ADJUST
   - 生成 Dr.Dre 的解释："X 天的可用时间不足 6 小时，节奏可能过紧。"

#### Neptune 的整合逻辑
1. **高风险段**（来自交通规划师）
   - 如果有高风险段 → REPLACE
   - 生成 Neptune 的解释："X 段跨城交通存在高风险。我为你准备了替代方案。"

2. **方案骨架**（来自总规划师）
   - 显示已选择的方案骨架
   - 强调路线哲学已确定

3. **替代方案**（来自安全守门人）
   - 如果有替代方案 → REPLACE
   - 生成 Neptune 的解释："路线本身没有问题，但部分路段需要替换。"

### 综合决策

根据三人格的 verdict，生成综合决策：
- 如果有 REJECT → 综合决策 = REJECT
- 如果有 ADJUST/REPLACE/NEED_CONFIRM → 综合决策 = NEED_CONFIRM
- 如果都是 ALLOW → 综合决策 = ALLOW

## UI 展示建议

### 三人格卡片
每个三人格显示为一张卡片：
```
┌─────────────────────────────────┐
│ 🐻‍❄️ Abu                        │
│ 我负责：这条路，真的能走吗？      │
├─────────────────────────────────┤
│ ✅ ALLOW                        │
│                                 │
│ 经过安全检查，当前方案在物理现实 │
│ 和合规性方面没有问题。我负责把你 │
│ 带去安全地带，这条路可以走。    │
│                                 │
│ 📊 证据：                       │
│ - 预算分析：交通类目正常         │
│ - 交通可达性：所有段可达         │
└─────────────────────────────────┘
```

### 综合决策条
```
┌─────────────────────────────────┐
│ ✅ 三人格一致通过，方案可行      │
│                                 │
│ 下一步：                        │
│ • 查看完整的行程详情            │
│ • 确认并锁定方案                │
└─────────────────────────────────┘
```

## 优势

1. **简化用户认知**：用户只需要理解三人格，不需要理解底层的能力模块
2. **增强信任感**：三人格有明确的角色定位和人格特征，更容易建立信任
3. **保持可解释性**：所有决策都有明确的来源（哪个三人格说的）
4. **隐藏复杂性**：底层的能力模块对用户透明，但内部仍然保持模块化

## 注意事项

1. **保持一致性**：三人格的解释风格要与现有系统保持一致
2. **证据可追溯**：虽然隐藏了底层模块，但证据仍然要可追溯
3. **调试友好**：在开发/调试模式下，可以显示底层模块的结果
